{
  "name": "vdo-ninja-custom",
  "version": "1.0.0",
  "private": true,
  "description": "Self-hosted, customizable VDO.Ninja client served via an Express server (default port 8366 = 'VDON')",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test",
    "precompress": "node scripts/precompress.js"
  },
  "engines": {
    "node": ">=18.11"
  },
  "dependencies": {
    "compression": "^1.7.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "supertest": "^7.2.2"
  }
}
